apiVersion: v1
kind: Pod
metadata:
  name: frontend-rm
  namespace: expense
spec:
  containers:
  - name: frontend-rm
    image: nginx
  resorces: 
    requests: 
      cpu: "100m"
      memory: "68 Mi"
    limits:
      memory: "128 Mi"
      cpu: "150m"
